let ftest : (x : number, y: number) => number;

ftest = function (x : number, y : number) : number {
    return x + y;
}

// ftest = (x, y) => x + y;
// ftest = (x : string, y) => x.substr(1) + y;
// ftest = (a, b) => a + b;
