let x : number;

x = 123;

function processString(s : string) {
    console.log(s);
}

// processString('hello');
// processString(10);
// processString({x : 'hello', y : 10});
// processString(undefined);
// processString(null);
